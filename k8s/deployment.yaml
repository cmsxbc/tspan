apiVersion: apps/v1
kind: Deployment
metadata:
  name: wyd-server
  namespace: wyd-system
  labels:
    app: wyd-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wyd-server
  template:
    metadata:
      labels:
        app: wyd-server
    spec:
      containers:
        - name: wyd-server
          image: wyd-server:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: DATABASE_URL
              value: "/app/data/data.db"
            - name: BIND_ADDR
              value: "0.0.0.0:8080"
            - name: RUST_LOG
              value: "wyd_server=info,tower_http=info"
            - name: WEB_PASSWORD
              value: "changeme"
              # TODO: replace with a Kubernetes Secret reference in production
              # valueFrom:
              #   secretKeyRef:
              #     name: wyd-web-auth
              #     key: password
          volumeMounts:
            - name: data
              mountPath: /app/data
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: wyd-data
