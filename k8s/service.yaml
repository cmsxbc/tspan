apiVersion: v1
kind: Service
metadata:
  name: wyd-server
  namespace: wyd-system
spec:
  selector:
    app: wyd-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
